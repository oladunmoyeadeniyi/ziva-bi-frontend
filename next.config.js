/** 
 * ============================================
 * NEXT.JS CONFIGURATION FILE
 * --------------------------------------------
 * This file allows us to configure the behavior
 * of the Next.js framework. For now, we enable:
 * 
 * - React Strict Mode (catches unsafe patterns)
 * 
 * Later, tenant theming, runtime configs,
 * and image domains will be added here.
 * ============================================
 */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;